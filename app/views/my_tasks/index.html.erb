<h1>My Tasks</h1>

<ul>
    <% @tasks.each do |task| %>
    <li>
        <%= task.title %>
    </li>
    <% end %>
</ul>

<p>
    <%= button_to 'Logout', :controller => 'sessions', :action => 'delete' %>
</p>